<%= render layout: 'index_table', locals: { cache_key: List.column_names.hash } do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @lists, as: :model %>
<% end %>

<%= paginate @lists %>
