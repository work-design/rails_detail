<%= form_for @note, as: :note, url: note_path(@note.detail_type, @note.detail_id), method: :patch, remote: true, html: { class: 'ui form' } do |f| %>
  <%= f.text_area :body, label: '标题:', class: 'attached' %>
  <%= f.submit '添加', class: 'ui button' %>
<% end %>