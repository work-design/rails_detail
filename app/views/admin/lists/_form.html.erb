<%= default_form_for [:admin, @list] do |f| %>
  <%= f.text_field :name, label: '名称：' %>
  <%= f.select :kind, options_for_select(List::KINDS), { label: '类别：' }, { include_blank: true } %>
  <%= f.submit %>
<% end %>

<script>
  $('select.dropdown').dropdown();
</script>