<div>

  <ul class="wiki-nav">
    <li>
      <span>节点Id：<%= section.id %></span>
      <%= link_to '添加节点', new_baike_section_path(@sort) %>
    </li>
     <li class="nav-dd">
          <span><%= section.name %></span>
          <% section.sorts.each do |sort| %>
            <span><%= link_to sort.name, baike_path(sort) %></span>
          <% end %>
     </li>
  </ul>

  <%= link_to '编辑', edit_section_path(section) %>

</div>




