.table-container
  .search-input.pull-right
    = text_field_tag :search, nil, placeholder: t('spina.search'), class: 'input-search input-rounded'
    .clear-input
      = icon('times-circle')

  %table.table.datatable
    %thead
      %tr
        %th= Attachment.human_attribute_name(:name)
        %th= Attachment.human_attribute_name(:size)
        %th= Attachment.human_attribute_name(:created_at)
        %th{colspan: 2}
      %tr
        %td{colspan: 5}
          = form_for [:site, @attachment], html: {multipart: true, class: "new_document"} do |f|
            = f.file_field :file, data: {customfileinput: true}

    %tbody.table-prepend-document
      = render partial: 'site/attachments/attachment', collection: @attachments