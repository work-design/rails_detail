#page_content.tab-content.active
  .table-container
    %table.table.table-form
      %tr
        %td
          = Page.human_attribute_name :title
        %td
          = f.text_field :title, placeholder: Page.human_attribute_name(:title_placeholder)

      = f.fields_for :page_parts, @page_parts.sort { |a,b| a.position(current_theme) <=> b.position(current_theme) } do |page_part_form|
        %tr.page-part{data: {name: page_part_form.object.name}}
          = render partial: "site/page_partables/#{page_part_form.object.page_partable_type.demodulize.underscore}_form", object: page_part_form
